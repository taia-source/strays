[
  {
    "id": "S1",
    "file": "src/eligible.ts",
    "what": "Remove the tax SANITY CEILING, letting a malformed taxPct (900) reach the cost model.",
    "find": "  if (token.taxPct > cfg.maxTaxPct) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S2",
    "file": "src/eligible.ts",
    "what": "Admit taxPct 0 — a failed API read read as the cheapest token on the venue.",
    "find": "  if (token.taxPct <= 0) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S3",
    "file": "src/cost.ts",
    "what": "Default gasPriceWei instead of refusing it — openhood's exact failure.",
    "find": "  if (args.gasPriceWei === undefined || args.gasPriceWei === null || args.gasPriceWei <= 0n) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 7
  },
  {
    "id": "S4",
    "file": "src/cost.ts",
    "what": "Charge the tax on ONE leg only, halving the modelled cost.",
    "find": "  const exitTaxWei = (args.positionWei * taxBps) / BPS_DENOMINATOR;",
    "replace": "  const exitTaxWei = 0n;",
    "caught": true,
    "failCount": 17
  },
  {
    "id": "S5",
    "file": "src/cost.ts",
    "what": "Replace the gas-price guard with a hardcoded anvil fallback — openhood's code, verbatim.",
    "find": "  if (args.gasPriceWei === undefined || args.gasPriceWei === null || args.gasPriceWei <= 0n) {",
    "replace": "  // @ts-expect-error sabotage: default instead of refusing\n  args = { ...args, gasPriceWei: args.gasPriceWei > 0n ? args.gasPriceWei : 1_019_000_000n };\n  if (false) {",
    "caught": true,
    "failCount": 7
  },
  {
    "id": "S6",
    "file": "src/bar.ts",
    "what": "Drop EDGE_MULTIPLE to 1 — the bar becomes cost, cleared by a third of a typical move.",
    "find": "export const EDGE_MULTIPLE = 2n;",
    "replace": "export const EDGE_MULTIPLE = 1n;",
    "caught": true,
    "failCount": 10
  },
  {
    "id": "S7",
    "file": "src/bar.ts",
    "what": "Remove the bar entirely: everything clears.",
    "find": "  const clears = args.expectedGainWei > 0n && args.expectedGainWei >= requiredWei;",
    "replace": "  const clears = true;",
    "caught": true,
    "failCount": 9
  },
  {
    "id": "S8",
    "file": "src/bar.ts",
    "what": "Remove the non-positive-gain guard, so 0 >= 0 fires a trade at zero cost.",
    "find": "  const clears = args.expectedGainWei > 0n && args.expectedGainWei >= requiredWei;",
    "replace": "  const clears = args.expectedGainWei >= requiredWei;",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S9",
    "file": "src/risk.ts",
    "what": "DELETE THE STOP LOSS — it never fires. This is meridian's actual state.",
    "find": "  const fired = moveBps <= -args.stopLossBps;",
    "replace": "  const fired = false;",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S10",
    "file": "src/risk.ts",
    "what": "Widen the stop by 10x so it effectively never fires on a real move.",
    "find": "  const fired = moveBps <= -args.stopLossBps;",
    "replace": "  const fired = moveBps <= -args.stopLossBps * 10n;",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S11",
    "file": "src/risk.ts",
    "what": "Remove the drawdown halt — a losing stray keeps hunting to zero.",
    "find": "  if (dd >= cfg.maxDrawdownBps) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S12",
    "file": "src/risk.ts",
    "what": "Let minOut return zero — a free MEV sandwich (RESEARCH §7c).",
    "find": "  if (args.expectedOut <= 0n) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S13",
    "file": "src/risk.ts",
    "what": "Accept a non-durable ledger — meridian's 'spend since last boot' (RESEARCH §7f).",
    "find": "  if (!ledger.durable) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S14",
    "file": "src/risk.ts",
    "what": "Make the memory ledger amnesiac: ignore the seed, so a restart loses all spend history.",
    "find": "  const records: SpendRecord[] = [...seed];",
    "replace": "  const records: SpendRecord[] = [];",
    "caught": true,
    "failCount": 12
  },
  {
    "id": "S15",
    "file": "src/risk.ts",
    "what": "Remove the window spend cap.",
    "find": "  if (spent + sizeWei > cfg.maxSpendPerWindowWei) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S16",
    "file": "src/risk.ts",
    "what": "Remove the count cap, so a retry storm under the value cap passes.",
    "find": "  if (count + 1 > cfg.maxEntriesPerWindow) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S17",
    "file": "src/risk.ts",
    "what": "MAKE THE EXIT REFUSABLE — mayExit returns false. The invariant the brief names.",
    "find": "export function mayExit(): true {\n  return true;\n}",
    "replace": "export function mayExit(): true {\n  // @ts-expect-error sabotage: an exit must never be refusable\n  return false;\n}",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S18",
    "file": "src/decide.ts",
    "what": "GATE THE EXIT BEHIND THE DRAWDOWN HALT — a halted stray can no longer sell.",
    "find": "  if (state.position !== undefined) {",
    "replace": "  if (state.position !== undefined && drawdownBps(state) < cfg.risk.maxDrawdownBps) {",
    "caught": null,
    "note": "PATTERN NOT FOUND — sabotage could not be applied"
  },
  {
    "id": "S19",
    "file": "src/decide.ts",
    "what": "Evaluate ENTRY before EXIT, so risk gates get a chance to run first.",
    "find": "    if (stop.fired) {",
    "replace": "    if (stop.fired && state.compartmentWei > 0n) {",
    "caught": null,
    "note": "PATTERN NOT FOUND — sabotage could not be applied"
  },
  {
    "id": "S20",
    "file": "src/signal.ts",
    "what": "Count the window from the SAMPLE COUNT, not the clock — openhood's 12x units bug.",
    "find": "  const elapsedSeconds = last.atSeconds - first.atSeconds;",
    "replace": "  const elapsedSeconds = (n - 1) * 3600;",
    "caught": true,
    "failCount": 13
  },
  {
    "id": "S21",
    "file": "src/signal.ts",
    "what": "Port openhood's 1440-minute lookback and 53bps sigma — the wrong asset class.",
    "find": "export const SIGMA_1H_BPS = 157n;",
    "replace": "export const SIGMA_1H_BPS = 53n;",
    "caught": true,
    "failCount": 7
  },
  {
    "id": "S22",
    "file": "src/signal.ts",
    "what": "Remove the long-only guard, so a crash is treated as an entry signal.",
    "find": "  const isBreakout = moveBps >= thresholdBps && moveBps > 0n;",
    "replace": "  const isBreakout = moveBps >= thresholdBps || moveBps <= -thresholdBps;",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S23",
    "file": "src/signal.ts",
    "what": "Remove the take-profit cost floor, so a target that cannot pay for its exit is allowed.",
    "find": "  const takeProfitBps = TAKE_PROFIT_BPS > costFloorBps ? TAKE_PROFIT_BPS : costFloorBps;",
    "replace": "  const takeProfitBps = TAKE_PROFIT_BPS;",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S24",
    "file": "src/eligible.ts",
    "what": "Remove the minimum-liquidity floor.",
    "find": "  if (token.marketCapWei < cfg.minMarketCapWei) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S25",
    "file": "src/eligible.ts",
    "what": "Remove the minimum-holders floor.",
    "find": "  if (!Number.isInteger(token.holders) || token.holders < cfg.minHolders) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S26",
    "file": "src/eligible.ts",
    "what": "Remove the age bounds.",
    "find": "  if (token.ageSeconds < cfg.minAgeSeconds) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S27",
    "file": "src/eligible.ts",
    "what": "Let assertTaxCeiling accept any configured ceiling, so a malformed taxPct reaches the cost model.",
    "find": "  if (!Number.isInteger(cfg.maxTaxPct) || cfg.maxTaxPct < 1 || cfg.maxTaxPct > MAX_PAD_TAX_PCT) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S28",
    "file": "src/decide.ts",
    "what": "Skip the eligibility filter in decide, so RULE 1 stops at the module boundary.",
    "find": "    if (!eligibility.ok) {",
    "replace": "    if (false && !eligibility.ok) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S29",
    "file": "src/decide.ts",
    "what": "Force a sale when the mark price is unreadable — a failed read becomes a realised loss.",
    "find": "      if (markPriceWei === undefined || markPriceWei <= 0n) {",
    "replace": "      if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S30",
    "file": "src/cost.ts",
    "what": "Reintroduce openhood's 5bps pool fee, which RESEARCH §2 proved is zero here.",
    "find": "  const totalWei = entryTaxWei + exitTaxWei + entryGasWei + exitGasWei;",
    "replace": "  const totalWei =\n    entryTaxWei + exitTaxWei + entryGasWei + exitGasWei + (args.positionWei * 5n) / 10_000n;",
    "caught": true,
    "failCount": 6
  },
  {
    "id": "S31",
    "file": "src/risk.ts",
    "what": "S12's PAIR: delete the SECOND minOut guard (the rounded-to-zero one) instead of the first.",
    "find": "  if (minOut <= 0n) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S32",
    "file": "src/risk.ts",
    "what": "Delete only the 100%-slippage guard; the rounding guard may still cover it.",
    "find": "  if (args.slippageBps < 0n || args.slippageBps >= BPS_DENOMINATOR) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S33",
    "file": "src/eligible.ts",
    "what": "Delete only the non-integer taxPct guard; the !== comparison may still cover it.",
    "find": "  if (!Number.isInteger(token.taxPct)) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S34",
    "file": "src/signal.ts",
    "what": "Delete only the zero-elapsed-time guard; the isqrt path may still produce no signal.",
    "find": "  if (!Number.isFinite(elapsedSeconds) || elapsedSeconds <= 0) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S35",
    "file": "src/signal.ts",
    "what": "Delete only the <2-points guard; the undefined-point guard may still cover it.",
    "find": "  if (n < 2) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S36",
    "file": "src/risk.ts",
    "what": "Delete only the compartment-affordability clamp; maxPositionWei may still cover it.",
    "find": "  const affordable = capped > state.compartmentWei ? state.compartmentWei : capped;",
    "replace": "  const affordable = capped;",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S37",
    "file": "src/cost.ts",
    "what": "Delete only the positive-position guard; a bps division by zero may throw anyway.",
    "find": "  if (args.positionWei <= 0n) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S38",
    "file": "src/screen.ts",
    "what": "DELETE THE SELL SIMULATION CHECK. 84 of 100 live tokens become buyable — the single highest-value check in the codebase.",
    "find": "  if (!sell.ok) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 7
  },
  {
    "id": "S39",
    "file": "src/screen.ts",
    "what": "Accept a sell that quotes but returns ZERO wei — a politely-reverting honeypot.",
    "find": "  if (sell.proceedsWei <= 0n) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S40",
    "file": "src/screen.ts",
    "what": "Remove the sniper/bundle ceiling — the concentration that naive top-10 hides behind.",
    "find": "  if (holders.sniperHeldPct > cfg.maxSniperHeldPct) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S41",
    "file": "src/screen.ts",
    "what": "Remove the top-10 concentration ceiling.",
    "find": "  if (holders.top10Pct > cfg.maxTop10Pct) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S42",
    "file": "src/screen.ts",
    "what": "Remove the creator-holdings ceiling — the supply that dumps on us.",
    "find": "  if (holders.creatorPct > cfg.maxCreatorPct) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S43",
    "file": "src/screen.ts",
    "what": "Treat a NaN sniper reading as 0% — a failed API read as wonderful decentralisation.",
    "find": "  if (!Number.isFinite(holders.sniperHeldPct) || holders.sniperHeldPct < 0) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S44",
    "file": "src/screen.ts",
    "what": "Check top-10 BEFORE snipers, letting a bundled token pass on a flattering top-10.",
    "find": "  if (holders.sniperHeldPct > cfg.maxSniperHeldPct) {",
    "replace": "  if (holders.sniperHeldPct > 100000) {",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S45",
    "file": "src/decide.ts",
    "what": "SKIP THE SCREEN INSIDE decide — the module is correct but never consulted (RESEARCH §7g).",
    "find": "    if (!screened.safe) {",
    "replace": "    if (false) {",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S46",
    "file": "src/score.ts",
    "what": "STOP SUBTRACTING TAX. Rank on the gross move, so a 10%-tax token ties a 1%-tax one.",
    "find": "  const netEdgeBps = input.expectedMoveBps - costBps;",
    "replace": "  const netEdgeBps = input.expectedMoveBps;",
    "caught": true,
    "failCount": 9
  },
  {
    "id": "S47",
    "file": "src/score.ts",
    "what": "Apply quality to a NEGATIVE edge, letting a multiplier flatter a losing trade toward zero.",
    "find": "      ? (netEdgeBps * depthBps * momentumBps) / (BPS_DENOMINATOR * BPS_DENOMINATOR)\n      : netEdgeBps;",
    "replace": "      ? (netEdgeBps * depthBps * momentumBps) / (BPS_DENOMINATOR * BPS_DENOMINATOR)\n      : (netEdgeBps * depthBps) / BPS_DENOMINATOR;",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S48",
    "file": "src/score.ts",
    "what": "Remove the quality clamp, letting a quality term ABOVE 1.0 inflate an edge.",
    "find": "  if (v < 0n) return 0n;\n  return v > BPS_DENOMINATOR ? BPS_DENOMINATOR : v;",
    "replace": "  return v;",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S49",
    "file": "src/score.ts",
    "what": "Drop the deterministic tiebreak, so arrival order (the LLM's channel) decides the winner.",
    "find": "    return a.address < b.address ? -1 : a.address > b.address ? 1 : 0;",
    "replace": "    return 0;",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S50",
    "file": "src/decide.ts",
    "what": "Enter the FIRST survivor rather than the best-ranked — the pre-rebuild behaviour.",
    "find": "  const ranked = rankCandidates(survivors.map((s) => s.score));",
    "replace": "  const ranked = survivors.map((s) => s.score);",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S51",
    "file": "src/decide.ts",
    "what": "Buy the least-bad candidate when NONE has a positive net edge.",
    "find": "    if (score.netEdgeBps <= 0n) {",
    "replace": "    if (false) {",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S52",
    "file": "src/decide.ts",
    "what": "REGRESSION: cost the exit against the CONFIG's tax rather than the position's own tier, understating a 10%-tax exit by ~1700bps.",
    "find": "      taxPct: position.taxPct,",
    "replace": "      taxPct: cfg.eligibility.maxTaxPct === 10 ? 1 : cfg.eligibility.maxTaxPct,",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S53",
    "file": "src/signal.ts",
    "what": "REGRESSION: truncate the take-profit cost floor DOWNWARD again, so a cost-bound target sits fractionally below the bar it exists to satisfy and every such trade is silently refused.",
    "find": "  const costFloorBps =\n    numerator / args.positionWei + (numerator % args.positionWei > 0n ? 1n : 0n);",
    "replace": "  const costFloorBps = numerator / args.positionWei;",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S54",
    "file": "src/decide.ts",
    "what": "IGNORE the configured edgeMultiple and silently fall back to the module constant, so a caller that raises its cost bar gets the default bar and never learns.",
    "find": "  const edgeMultiple = cfg.edgeMultiple ?? EDGE_MULTIPLE;",
    "replace": "  const edgeMultiple = EDGE_MULTIPLE;",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S55",
    "file": "src/decide.ts",
    "what": "Let the ENTRY BAR and the TAKE-PROFIT FLOOR disagree: the bar uses the configured multiple while the target keeps the default, so a trade may fire against a target that cannot pay for it.",
    "find": "      multiple: edgeMultiple,",
    "replace": "      multiple: 1n,",
    "caught": true,
    "failCount": 6
  },
  {
    "id": "S56",
    "file": "src/risk.ts",
    "what": "REINSTATE THE ONE-POSITION RULE. §10.5 measured this at 17 of 72 opportunities taken and a Welch t of 1.16 — not significant. It is the single most expensive line this package ever shipped.",
    "find": "  const slot = firstFreeSlot(state, configuredSlots);",
    "replace": "  const slot = state.positions.length > 0 ? undefined : firstFreeSlot(state, configuredSlots);",
    "caught": true,
    "failCount": 8
  },
  {
    "id": "S57",
    "file": "src/risk.ts",
    "what": "Let a NINTH position through, past the contract's fixed 8-slot array — the keeper commits the spend to the ledger and then eats a NoFreeSlot revert.",
    "find": "  if (slot === undefined) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S58",
    "file": "src/risk.ts",
    "what": "Ignore the configured slot count so a $10 stray (4 slots) behaves like a $20 one, overcommitting capital it does not have.",
    "find": "    cfg.maxPositions < MAX_POSITIONS ? cfg.maxPositions : MAX_POSITIONS;",
    "replace": "    MAX_POSITIONS;",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S59",
    "file": "src/risk.ts",
    "what": "Size against the FREE compartment rather than free+committed, so the fraction compounds downward and the stray silently caps itself at ~3 positions while holding money and free slots.",
    "find": "  const tradingCapitalWei = state.compartmentWei + committedWei(state);",
    "replace": "  const tradingCapitalWei = state.compartmentWei;",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S60",
    "file": "src/risk.ts",
    "what": "Let the position size exceed FREE cash, funding a new slot out of capital that is already deployed.",
    "find": "  const affordable = capped > state.compartmentWei ? state.compartmentWei : capped;",
    "replace": "  const affordable = capped;",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S61",
    "file": "src/risk.ts",
    "what": "Scan slots highest-first, disagreeing with StrayVault.hunt's lowest-first scan — the indexer's watermark then attaches to a different slot than the chain filled.",
    "find": "  for (let i = 0; i < bound; i++) {\n    if (!taken.has(i)) return i;\n  }",
    "replace": "  for (let i = bound - 1; i >= 0; i--) {\n    if (!taken.has(i)) return i;\n  }",
    "caught": true,
    "failCount": 8
  },
  {
    "id": "S62",
    "file": "src/risk.ts",
    "what": "Allow the SAME token in two slots — eight slots become one idea eight times, multiplying exposure without adding an observation.",
    "find": "  if (args.token !== undefined) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S63",
    "file": "src/trail.ts",
    "what": "MAKE THE PEAK WATERMARK NON-MONOTONE, so it follows the price down and the trailing stop becomes a very slow market order that never fires.",
    "find": "  return markPriceWei > currentPeakWei ? markPriceWei : currentPeakWei;",
    "replace": "  return markPriceWei;",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S64",
    "file": "src/trail.ts",
    "what": "Let a failed price read (0 wei) reset the watermark — RESEARCH §7f's 'spend since last boot' in a new costume, silently disarming the only exit the strategy has.",
    "find": "  if (markPriceWei <= 0n) {\n    throw new Error(\n      `refusing to update a peak watermark",
    "replace": "  if (false) {\n    throw new Error(\n      `refusing to update a peak watermark",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S65",
    "file": "src/trail.ts",
    "what": "Accept a ZERO peak watermark, so a reset or unset watermark re-anchors the stop to the current price rather than being refused.",
    "find": "  if (peakPriceWei <= 0n) {",
    "replace": "  if (false && peakPriceWei <= 0n) {",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S66",
    "file": "src/trail.ts",
    "what": "Treat an unreadable mark (0 wei) as a 100% fall from the peak, so a single API blip sells every position at once.",
    "find": "  if (markPriceWei <= 0n) {\n    throw new Error(\n      `refusing to evaluate a trailing stop against a mark",
    "replace": "  if (false) {\n    throw new Error(\n      `refusing to evaluate a trailing stop against a mark",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S67",
    "file": "src/trail.ts",
    "what": "Make the trailing stop exclusive (> instead of >=), so it is one tick late by construction on an asset that can halve inside a tick.",
    "find": "  const fired = fallFromPeakBps >= trailBps;",
    "replace": "  const fired = fallFromPeakBps > trailBps + 1n;",
    "caught": true,
    "failCount": 7
  },
  {
    "id": "S68",
    "file": "src/trail.ts",
    "what": "Measure the fall from ENTRY rather than from the PEAK — i.e. revert to a level stop, the family RESULTS §10 refuted.",
    "find": "          : ((peakPriceWei - markPriceWei) * BPS_DENOMINATOR) / peakPriceWei;",
    "replace": "          : ((peakPriceWei - markPriceWei) * BPS_DENOMINATOR) / markPriceWei;",
    "caught": null,
    "note": "PATTERN NOT FOUND — sabotage could not be applied"
  },
  {
    "id": "S69",
    "file": "src/trail.ts",
    "what": "Narrow the 50% trail back to the refuted -235bps level, shaking the strategy out of exactly the moves that made the money.",
    "find": "export const TRAIL_BPS = 5000n;",
    "replace": "export const TRAIL_BPS = 235n;",
    "caught": true,
    "failCount": 7
  },
  {
    "id": "S70",
    "file": "src/decide.ts",
    "what": "SKIP THE ENTRY-AGE GATE inside decide — age.ts is correct but never consulted, which is RESEARCH §7g exactly.",
    "find": "    if (!age.inWindow) {",
    "replace": "    if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S71",
    "file": "src/age.ts",
    "what": "Remove the entry-window FLOOR, entering tokens before any sell of our size has been absorbed — RESEARCH-PROFITABLE-AGENTS §9's named kill condition.",
    "find": "  if (swapCount < cfg.entrySwapIndex) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S72",
    "file": "src/age.ts",
    "what": "Remove the entry-window CEILING, entering at doses whose measured median is negative (-845bps at swap 100, -3,302 at swap 500).",
    "find": "  if (swapCount > cfg.maxEntrySwapIndex) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S73",
    "file": "src/age.ts",
    "what": "Treat a failed swapCount read as swap 0 — the earliest and most attractive dose, so every unreadable token looks maximally good.",
    "find": "  if (!Number.isInteger(swapCount) || swapCount < 0) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S74",
    "file": "src/age.ts",
    "what": "Move the shipped entry dose past where the measured median crosses zero.",
    "find": "export const ENTRY_SWAP_INDEX = 20;",
    "replace": "export const ENTRY_SWAP_INDEX = 120;",
    "caught": true,
    "failCount": 55
  },
  {
    "id": "S75",
    "file": "src/age.ts",
    "what": "Price every candidate at the BEST dose regardless of its actual age, feeding the cost bar an expectation the token has not earned.",
    "find": "    if (entry[0] <= swapCount) chosen = entry;",
    "replace": "    if (entry[0] <= 5) chosen = entry;",
    "caught": true,
    "failCount": 10
  },
  {
    "id": "S76",
    "file": "src/age.ts",
    "what": "Drop the honesty caveat from the admission reason, so /logs reports +4,410bps as a promise rather than as an out-of-sample-positive, credible:false hypothesis.",
    "find": "      \"credible:false at 183 cumulative trials, n=72, and the cost model is incomplete in the \" +",
    "replace": "      \"\" +",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S77",
    "file": "src/hook.ts",
    "what": "ACCEPT AN ARBITRARY HOOK. A v4 hook runs inside the swap with the pool's permissions, so this puts an attacker-controlled contract on the money path.",
    "find": "  return KNOWN_HOOKS.some((known) => known.toLowerCase() === normalised);",
    "replace": "  return true;",
    "caught": true,
    "failCount": 9
  },
  {
    "id": "S78",
    "file": "src/hook.ts",
    "what": "Drop the SECOND hook from the allowlist, reproducing v1's blindness to 44 of 111 tokens including LEVCAT, INTERN and Seriouscat.",
    "find": "export const KNOWN_HOOKS: readonly [string, string] = [HOOK_PRIMARY, HOOK_SECONDARY];",
    "replace": "export const KNOWN_HOOKS: readonly [string, string] = [HOOK_PRIMARY, HOOK_PRIMARY];",
    "caught": true,
    "failCount": 10
  },
  {
    "id": "S79",
    "file": "src/hook.ts",
    "what": "Make the allowlist case-SENSITIVE, so a lowercase read of a legitimate hook is refused and the keeper declines every trade from one data source.",
    "find": "  const normalised = hook.trim().toLowerCase();\n  return KNOWN_HOOKS.some((known) => known.toLowerCase() === normalised);",
    "replace": "  const normalised = hook;\n  return KNOWN_HOOKS.some((known) => known === normalised);",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S80",
    "file": "src/hook.ts",
    "what": "Let canonicalHook launder an unknown address into a canonical-looking one by skipping the allowlist assertion.",
    "find": "export function canonicalHook(hook: string): string {\n  assertKnownHook(hook);",
    "replace": "export function canonicalHook(hook: string): string {\n  if (false) assertKnownHook(hook);",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S81",
    "file": "src/eligible.ts",
    "what": "Skip the per-token hook check in eligibility, so an unaddressable or hostile pool reaches the quoter and the router.",
    "find": "  if (!isKnownHook(token.hook)) {",
    "replace": "  if (false) {",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S82",
    "file": "src/decide.ts",
    "what": "RETURN on the first unreadable mark instead of continuing the scan — one missing price then disarms every later position's trailing stop, on every tick, until that read recovers.",
    "find": "        continue;\n      }\n\n      /*\n       * ══ THE EXIT RULE.",
    "replace": "        return { kind: \"hold\", reason: notes[0] ?? \"\" };\n      }\n\n      /*\n       * ══ THE EXIT RULE.",
    "caught": true,
    "failCount": 2
  },
  {
    "id": "S83",
    "file": "src/decide.ts",
    "what": "Return HOLD after the exit scan instead of falling through to entry — a stray with free slots stops hunting the moment it holds anything, which is the one-position rule reintroduced at the decision layer.",
    "find": "    holdNotes.push(...notes);",
    "replace": "    return { kind: \"hold\", reason: notes.join(\" | \") };",
    "caught": true,
    "failCount": 3
  },
  {
    "id": "S84",
    "file": "src/decide.ts",
    "what": "Report the wrong SLOT on an exit. flee() reads the token, hook and tickSpacing out of the slot it is given, so this sells a DIFFERENT position at this one's minOut.",
    "find": "          slot: position.slot,",
    "replace": "          slot: 0,",
    "caught": true,
    "failCount": 5
  },
  {
    "id": "S85",
    "file": "src/decide.ts",
    "what": "Match mark prices case-SENSITIVELY, so an EIP-55-cased read misses the map and silently disarms a stop (a miss is indistinguishable from a failed read, and a failed read holds).",
    "find": "      const wanted = token.toLowerCase();",
    "replace": "      return undefined;\n      const wanted = token.toLowerCase();",
    "caught": true,
    "failCount": 1
  },
  {
    "id": "S86",
    "file": "src/decide.ts",
    "what": "Feed the cost bar the refuted momentum projection again instead of the measured held-out median.",
    "find": "    const expectedGainWei = (gate.sizeWei * measuredMedianNetBps(age.swapCount, ageCfg)) / 10_000n;",
    "replace": "    const expectedGainWei = signal.expectedGainWei;",
    "caught": true,
    "failCount": 4
  },
  {
    "id": "S87",
    "file": "src/decide.ts",
    "what": "Restore the momentum BREAKOUT as an entry gate, re-buying the top run-up quintile whose measured forward net is -5,999bps.",
    "find": "    const age = withinEntryWindow(candidate.token.swapCount, ageCfg);",
    "replace": "    const age = withinEntryWindow(candidate.token.swapCount, ageCfg);\n    if (evaluateEntry({ history: candidate.history, positionWei: gate.sizeWei, takeProfitBps: 471n }).direction !== \"long\") continue;",
    "caught": true,
    "failCount": 3
  }
]