{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "corepack enable && pnpm install --no-frozen-lockfile && pnpm --filter @strays/hunt build && pnpm --filter @strays/indexer build"
  },
  "deploy": {
    "startCommand": "node apps/indexer/dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
